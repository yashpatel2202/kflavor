/// kflavor — CLI utilities to generate and apply Flutter flavor configurations and
/// platform artifacts (Android/iOS). Export the top-level `KFlavorRunner` so
/// consumers can run the generator programmatically or from an executable.
library;

export 'src/runner.dart' show KFlavorRunner;
